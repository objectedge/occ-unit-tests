import ko from 'knockout';
import ccLogger from 'ccLogger';
import navigation from 'navigation';
import pubsub from 'pubsub';

export default {
  onLoad() {
    console.log('loading sample app level');
  },
  returnTrue() {
    return true;
  }
};
